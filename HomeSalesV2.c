#include <stdio.h>

int main(
  
    char salesperson[]; = {"Danielle", "Edward", "Francis", "Z"};
    char sp; 
    char spInitial[]; = {"D", "E", "F"};
    int sale[];
    int total[];
    
    printf("Enter the name of the Salespersons\n");
    scanf("%c", &sp); 
    
    while (sp != "Z") {
        
    
        switch (salesperson[]) {

            case "Danielle"
            
            printf("How much did they make?\n");
            scanf("%i", &sale[0]);
            printf("Sale:"sale[0]);
        }

    }

)